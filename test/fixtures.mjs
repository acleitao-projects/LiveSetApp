import {mkdir,writeFile} from 'node:fs/promises';
const defaultSampleRate=8000;
export function wavFixture(seconds=8,frequency=440,{impulseFrame=-1,sampleRate=defaultSampleRate}={}){const count=sampleRate*seconds,data=new Uint8Array(count*2),view=new DataView(data.buffer);for(let i=0;i<count;i++)view.setInt16(i*2,i===impulseFrame?30000:Math.round(Math.sin(2*Math.PI*frequency*i/sampleRate)*12000),true);const out=new Uint8Array(44+data.length),v=new DataView(out.buffer),put=(o,s)=>[...s].forEach((c,i)=>v.setUint8(o+i,c.charCodeAt(0)));put(0,'RIFF');v.setUint32(4,36+data.length,true);put(8,'WAVE');put(12,'fmt ');v.setUint32(16,16,true);v.setUint16(20,1,true);v.setUint16(22,1,true);v.setUint32(24,sampleRate,true);v.setUint32(28,sampleRate*2,true);v.setUint16(32,2,true);v.setUint16(34,16,true);put(36,'data');v.setUint32(40,data.length,true);out.set(data,44);return out;}
export async function createFixtures(dir='test/fixtures'){await mkdir(dir,{recursive:true});const files=[];for(const [name,freq,seconds=30] of [['track-a',330],['track-b',440],['track-c',550],['track-d',660],['track-e',770],['track-long',275,60]]){const path=`${dir}/${name}.wav`;await writeFile(path,wavFixture(seconds,freq));files.push(path);}for(const [name,seconds] of [['separation-source',8],['separation-short',4]]){const path=`${dir}/${name}.wav`;await writeFile(path,wavFixture(seconds,440,{sampleRate:44100}));files.push(path);}const stems=[['vocals',220],['guitar',330],['bass',110],['drums',440],['other',550]],stemDir=`${dir}/stems`;await mkdir(stemDir,{recursive:true});for(const [name,freq] of stems){const path=`${stemDir}/${name}.wav`;await writeFile(path,wavFixture(30,freq,{impulseFrame:24000,sampleRate:48000}));files.push(path);}const invalidDir=`${dir}/invalid-stems`;await mkdir(invalidDir,{recursive:true});for(const [name,freq] of stems){const path=`${invalidDir}/${name}.wav`;await writeFile(path,wavFixture(30,freq,{impulseFrame:24000,sampleRate:name==='drums'?8000:48000}));files.push(path);}return files;}
if(process.argv[1]?.endsWith('fixtures.mjs'))console.log((await createFixtures()).join('\n'));
